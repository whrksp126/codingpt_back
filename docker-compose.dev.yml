version: '3.8'

services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3100:3100"
    environment:
      - NODE_ENV=development
    env_file:
      - .env.dev
    restart: unless-stopped 