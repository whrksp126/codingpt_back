version: '3.8'

services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile.stg
    ports:
      - "3100:3100"
    environment:
      - NODE_ENV=staging
    env_file:
      - .env.stg
    restart: unless-stopped 