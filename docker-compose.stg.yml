version: '3.8'

services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile.stg
    ports:
      - "5101:5100"
    environment:
      - NODE_ENV=staging
    env_file:
      - .env.stg
    restart: unless-stopped 